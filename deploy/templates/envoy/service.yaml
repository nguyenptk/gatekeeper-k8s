apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.envoy.service.name }}
spec:
  selector:
    app: envoy
  ports:
    - name: http
      port: {{ .Values.envoy.service.port }}
      targetPort: {{ .Values.envoy.service.port }}
    - name: admin
      port: 9901
      targetPort: 9901
